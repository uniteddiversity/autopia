<%= partial :block, locals: {
  title: activity.name,
  image: activity.image,
  view_url: "/activities/#{activity.id}",
  edit_url: "/activities/#{activity.id}/edit",
  admin: activity_admin?(activity)
  
  } %>
