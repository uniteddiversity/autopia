
<% if organisation.persisted? && organisation_assistant? %>
  <div class="mb-3">
    <%= ul_nav [
      ['View organisation', "/organisations/#{organisation.id}"],
      (['Edit organisation', "/organisations/#{organisation.id}/edit"] if organisation_admin?),      
      (['Event stats', "/organisations/#{organisation.id}/events/stats"]  if organisation_admin?),
      ['Mailer', "/organisations/#{organisation.id}/pmails"]      
      ].compact, ul_class: 'nav nav-tabs', li_class: 'nav-item', a_class: 'nav-link' %>
  </div>
<% end %>

