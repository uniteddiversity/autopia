
<p style="font-size: 20px; margin: 10px 0 0 0">
  Automatically accepting applications with <%=@group.threshold%> proposers + supporters
</p>

<% unless @group.fixed_threshold %>
  What do you think the threshold should be?
  <% form_tag "/h/#{@group.slug}/threshold", :style => 'display: inline' do %>
    <%= number_field_tag :desired_threshold, :min => 0, :class => 'form-control', :value => @membership.desired_threshold, :style => 'display: inline-block; width: 4em', :onchange => '$(this.form).submit()' %>
  <% end %> 
<% end %>