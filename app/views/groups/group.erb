
<%= partial :'groups/header' %>

<p>         
  <% if @membership.admin? %>    
    <a class="btn btn-outline-primary" href="/h/<%=@group.slug%>/edit">Edit group</a>
    <a class="btn btn-outline-primary" href="/h/<%=@group.slug%>/balance">Balance: <%=@group.currency_symbol%><%="#{'%.2f' % @group.balance}"%></a>
  <% end %>  
  <% if @membership.unsubscribed %>
    <a class="btn btn-outline-primary" href="/h/<%=@group.slug%>/subscribe">Turn on email notifications of key events</a>
  <% else %>
    <a class="btn btn-outline-primary" href="/h/<%=@group.slug%>/unsubscribe">Turn off email notifications of key events</a>
  <% end %>              
  <a class="btn btn-outline-primary" data-confirm="Are you sure you want to leave the group?" href="/h/<%=@group.slug%>/leave">Leave group</a>
</p>
<p>
  Non-members can <%=  @group.enable_applications ? 'apply' : 'join' %> at <a href="<%="#{ENV['BASE_URI']}/h/#{@group.slug}/#{@group.enable_applications ? 'apply' : 'join'}"%>"><%="#{ENV['BASE_URI']}/h/#{@group.slug}/#{@group.enable_applications ? 'apply' : 'join'}"%></a>
</p>                 

<div class="row">
  <div class="col-sm-6">
    <div data-pagelet-url="/h/<%=@group.slug%>/todos" style="margin-top: 20px">
      <%= partial :'groups/todos' %>
    </div>    
    <% if @group.intro_for_members %>  
      <hr />
      <%= @group.intro_for_members %>        
    <% end %>  
    <h2>Online now</h2>
    <div class="image-wall">
      <% @group.members.where(:last_active.gt => 5.minutes.ago).each { |account| %>
        <%= partial :'accounts/square', :locals => {:account => account, :content => (if account.last_active; "#{time_ago_in_words account.last_active} ago"; end), :width => '75px'} %>
      <% } %>
    </div>
  </div>  
  <div class="col-sm-6">
    <div data-pagelet-url="/h/<%=@group.slug%>/newsfeed" data-pusher-channel="notifications.<%=@group.slug%>" style="margin-top: 10px"></div>
  </div>
</div>